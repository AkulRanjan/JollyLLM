# Synthetic fixtures

The current fixtures are synthetic and contain no real legal-source text or personal data.

The file src/legal_graph/fixtures.py defines the test graph. Use the local export API to write an inspectable graph and content-addressed manifest under a local artifact directory. Generated artifacts are ignored by Git and never uploaded.
